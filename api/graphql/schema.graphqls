

enum BonusMalusUsage{
  PRIVATE
  PROFESIONNAL
}

type Query {
  "Compute the Belgium BonusMalus score as a value between -2 (best) and 22 (worst)"
  bonusMalusScore(drivingYears: Int!, accidents: Int!, usage: BonusMalusUsage!): Int!
}

